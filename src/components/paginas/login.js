import React from "react"; //siempre se importa react
import B_Login from "../B_Login"; //importamos el body de login 


function Login(){ //creamos la funcion 
    return(// agrgamos el return para que me retones los datos que van dentro del mismo 
    <div>

<B_Login // llamamos el body del login que se importo anteriormnte


/>

</div>
    )   
}
export default Login // importamos la funcion para poder importarla en otro paquete